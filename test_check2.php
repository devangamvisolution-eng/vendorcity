<?php require 'vendor/autoload.php'; $app = require_once 'bootstrap/app.php'; $kernel = $app->make(Illuminate\Contracts\Console\Kernel::class); $kernel->bootstrap(); $visit = DB::table('ci_order_visits')->where('visit_date', '2026-12-24')->first(); if($visit) { $all_visits = DB::table('ci_order_visits')->where('order_id', $visit->order_id)->orderBy('visit_date', 'asc')->get(); print_r($all_visits); $order = DB::table('ci_orders')->where('order_id', $visit->order_id)->first(); print_r($order); $order_item = DB::table('ci_order_item')->where('order_id', $visit->order_id)->first(); print_r($order_item); }
